[Unit]
Description=Coolant Monitor Server
After=multi-user.target

[Service]
Type=forking
ExecStart=/usr/bin/tmux new-session -s CoolantMonitor -d -c <path-containing-executable> <path-of-executable>
ExecStop=/usr/bin/tmux kill-session -t CoolantMonitor
Restart=always
User=<user>
WorkingDirectory=<path-containing-executable>

[Install]
WantedBy=multi-user.target
